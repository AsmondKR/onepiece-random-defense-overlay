<local:OverlayWindowBase x:Class="OrandOverlay.StatsOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OrandOverlay"
        Width="640" Height="240" Top="440" Left="1180"
        FontFamily="Malgun Gothic"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize">
  <!-- 테스트3: 세로 스택이 아니라 가로 수치 바. 조합 안내는 추천 창 지금 구역. -->
  <Border Background="#F2080A0E" BorderBrush="#FF232833" BorderThickness="1">
    <Grid Margin="10,8,10,8">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <Grid Background="Transparent" Cursor="SizeAll" MouseLeftButtonDown="DragArea_OnMouseLeftButtonDown">
        <Grid.ColumnDefinitions>
          <ColumnDefinition/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="패 수치" Foreground="#FFF0C75E" FontSize="13" FontWeight="Bold"/>
        <Border Grid.Column="1" x:Name="ClickThroughBadge" Visibility="Collapsed"
                Background="#331C2028" CornerRadius="2" Padding="6,2" VerticalAlignment="Center">
          <TextBlock Text="클릭 통과" Foreground="#FF8B93A7" FontSize="10"/>
        </Border>
      </Grid>
      <UniformGrid Grid.Row="1" x:Name="CoreKpiPanel" Columns="3" Margin="0,6,0,0"/>
      <ScrollViewer Grid.Row="2" Margin="0,6,0,0" VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Disabled">
        <StackPanel>
          <WrapPanel x:Name="CurrentStatsPanel"/>
          <TextBlock x:Name="EmergencyHeader" Text="긴급소집" Foreground="#FFF0C75E"
                     FontSize="10" FontWeight="SemiBold" Margin="0,8,0,4" Visibility="Collapsed"/>
          <WrapPanel x:Name="EmergencyPanel" Visibility="Collapsed"/>
          <TextBlock Text="정리" Foreground="#FF8B93A7" FontSize="10"
                     FontWeight="SemiBold" Margin="0,8,0,4"/>
          <WrapPanel x:Name="RareRerollPanel"/>
          <TextBlock x:Name="SpecialHeader" Text="특수함" Foreground="#FF8B93A7" FontSize="10"
                     FontWeight="SemiBold" Margin="0,6,0,4" Visibility="Collapsed"/>
          <WrapPanel x:Name="SpecialPanel" Visibility="Collapsed"/>
          <TextBlock x:Name="GreenBloodHeader" Text="그린블러드" Foreground="#FF3DDC84" FontSize="10"
                     FontWeight="SemiBold" Margin="0,6,0,4" Visibility="Collapsed"/>
          <WrapPanel x:Name="GreenBloodPanel" Visibility="Collapsed"/>
        </StackPanel>
      </ScrollViewer>
    </Grid>
  </Border>
</local:OverlayWindowBase>
